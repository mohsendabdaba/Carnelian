// import { Height } from "@material-ui/icons";

const Logo = (props) => (
  <img
    alt="Logo"
    src="/static/logo.png"
    {...props}
    width="100px"
    height="40px"
  />
);

export default Logo;
